[package]
name = "com-api-runtime-iceoryx"
version = "0.1.0"
edition = "2024"
publish = ["common"]
license = "Apache-2.0"

[dependencies]
com-api-concept = { workspace = true, features = ["iceoryx"] }

[dev-dependencies]
futures = "0.3"
